[project]
name = "langflow-ollama-ai-chatbot"
version = "0.1.0"
description = ""
authors = [
    {name = "raylai",email = "chunjueilai@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.11, <3.12"
dependencies = [
]

[tool.poetry]
packages = [{include = "langflow_ollama_ai_chatbot", from = "src"}]

[tool.poetry.scripts]
langflow-ollama-ai-chatbot = "langflow-ollama-ai-chatbot.main:main"

[tool.poetry.group.test.dependencies]
flake8 = "^7.3.0"
isort = "^7.0.0"
pydocstyle = "^6.3.0"
pre-commit = "^4.5.0"
commitizen = "^4.10.0"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
